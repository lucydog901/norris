
<div class="hero">
  <div class="frosty2">
    <h1 align="left">Chuck Norris Facts</h1>
    <p align="left">A compendium of Chuck Norris facts of questionable validity, and unquestionable profanity.</p>
  </div>
   <div class="row">
    <div class="frosty col-sm-8 offset-2">
      <div class="mx-auto mt-5" style="width: 400px"><br />
        <p align="center">Search these unproven facts by keyword, or refresh the page to display one random fact.</p><br /><br />
        <%= form_with(url: search_path, method: 'get', local: true) do %>
          <div class="form-group">
            <%= label_tag :keyword, 'Search for a keyword '%>
            <%= text_field_tag :keyword, nil, placeholder: 'Eg. Fight', class: 'form-control' %>
          </div>
          <%= button_tag 'Search', class: 'btn btn-success btn-block' %>
          <div class="col-lg-12 mt-5">
            <% if flash[:alert] %>
              <span class="error_msgs"> <% if alert %>
        <p class="alert alert-success">
            <%= alert %>
        </p> <% end %> </span>
                 
               </div>
            <% end %>
        <% end %>
      
      </div>
    </div>
</div>



<div class="container">
  <div class="row2">
  <div class="col-lg-12 mt-5">
      <div class="mx-auto mt-5" style="width: 600px">  
        <% if @jokes %><h2 align="left">Results:</h1>
          <% @jokes.each do |joke| %>
            <p><%= joke["value"] %></p>
          <% end %>
        <% end %>
      </div>
    </div>
    </div>
    </div>


